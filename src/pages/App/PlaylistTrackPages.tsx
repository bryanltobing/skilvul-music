import AppWrapper from 'components/_common/wrapper/AppWrapper';
import { PlaylistTrackContainer } from 'containers/App/PlaylistTrackContainer';

const PlaylistTrackPages = () => {
  return (
    <AppWrapper>
      <PlaylistTrackContainer />
    </AppWrapper>
  );
};

export default PlaylistTrackPages;
